{
  "name": "react-from-svg",
  "version": "0.1.0",
  "license": "MIT",
  "keywords": [
    "react",
    "react-native",
    "reason-react",
    "bs-react",
    "svg",
    "react-native-svg",
    "react-native-web",
    "bucklescript",
    "react-native",
    "svg-to-react",
    "svgr"
  ],
  "bin": {
    "react-from-svg": "./bin.js"
  },
  "files": [
    "src",
    "!test",
    "bin.js",
    "bsconfig.json"
  ],
  "scripts": {
    "re:clean": "bsb -clean-world",
    "re:build": "bsb -make-world",
    "svg:format": "find test/components -name \"*.re\" | grep -v \"node_modules\" | xargs bsrefmt --in-place",
    "svg": "trash test/components && ./bin.js test test/components && yarn svg:format",
    "test": "yarn re:build && yarn svg",
    "prepare": "yarn re:clean && yarn re:build"
  },
  "dependencies": {
    "bs-platform": "^5.0.0",
    "glob": "^7.1.3",
    "mkdirp": "^0.5.1",
    "reason-future": "^2.4.0"
  },
  "devDependencies": {
    "npmpub": "^4.1.0",
    "reason-react": "https://github.com/reasonml/reason-react.git#0.7.0",
    "trash-cli": "^1.4.0"
  }
}
